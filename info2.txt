Some informations:

	[PWD]

		O pwd utilizado é o /bin/pwd, não é o mesmo que no bash, em que o pwd executado é o de builtin da shell.

		Ao seja, há algumas diferenças, como as menssagens de error em caso da seguinte execução: pwd pwd.